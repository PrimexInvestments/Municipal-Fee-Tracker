# Scraper + site (the standalone path)
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
pdfplumber>=0.11

# Only needed if you also keep the spreadsheet
# (build_workbook.py, or build_site.py --from-workbook)
openpyxl>=3.1
